version: "3"
services:
  psql-db:
    image: postgres
    environment:
      - POSTGRES_USER=hamzaio
      - POSTGRES_PASSWORD=hamzapass
      - POSTGRES_DB=akamarket
  marjane:
    build: .
    ports:
      - 8080:8080
    depends_on:
      - psql-db